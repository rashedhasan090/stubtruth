# Demo Widget Kit

A tiny demo for **stubtruth**.

## Features

- Provides `render_widget` for HTML snippets
- Implements `WidgetCache` for memoized renders
- Supports `export_pdf` for archival dumps
- Offers `ping_health` for uptime checks

## Usage

```bash
python -m demo_pkg
```
